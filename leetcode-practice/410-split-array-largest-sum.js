/**
 * @param {number[]} nums
 * @param {number} m
 * @return {number}
 */
 var splitArray = function(nums, m) {

    if (!nums.length) return 0;

    // use BST?

    // splitting array into m arrays

    // take the sum of each array and compare 
    // tracking largest sum => memoizing sum values 

    // iterate through potential array configurations

    // return minimize LARGEST sum
};